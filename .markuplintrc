{
  "extends": [
    "markuplint:recommended"
  ],
  "nodeRules": [
    {
      "selector": "html",
      "rules": {
        "invalid-attr": {
          "options": {
            "allowAttrs": ["prefix"]
          }
        }
      }
    },
    {
      "selector": "#MathJax-script",
      "rules": {
        "required-attr": ["async"]
      }
    }
  ]
}
